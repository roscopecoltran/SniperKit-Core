---
cmake:
  hunter:
    registry:
      - package_name: 
        config_mode:
          status: True
          mapping:
            main:
              config_name:          
            modules:
              - name:
                config_name: 
        cmake:
          build:
            args:
          find:
            required: True
        meta: # meta are generated by a python script in order to help us to set the CMake mapping

